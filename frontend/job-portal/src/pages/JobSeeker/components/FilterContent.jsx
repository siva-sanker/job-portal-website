import React from 'react'

const FilterContent = ({toggleSection,clearAllFilters,expandedSections,filers,handleFilterChange}) => {
  return (
    <div>
      filtercontent
    </div>
  )
}

export default FilterContent
